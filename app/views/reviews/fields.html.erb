<div class="well" >
<!-- id="<%#= dom_id(review) %>"> -->
  <%= review.body %> |
  <!-- Created By: <%= review.user.full_name %> on -->
    <%= formatted_date(review.created_at) %>
  <% if can? :destroy, review  %>

    <%= link_to "Delete", product_review_path(@product, review), method: :delete,
                data: {confirm: "Are you sure you want delete the review?"},
                remote: true %>
  <% end %>
</div>

<h2>Add Reviews</h2>
<div id="reviews">
  <%= f.simple_fields_for :reviews do |rv| %>
    <%= render "review_fields", f: rv  %>
  <% end %>
  <div class="links">
    <%= link_to_add_association 'add reward', f, :rewards %>
  </div> -->

<div class="nested-fields">
  <%= f.input :title %>
  <%= link_to_remove_association "remove reward", f %>
  <hr>
</div>
